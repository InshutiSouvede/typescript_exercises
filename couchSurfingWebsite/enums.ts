export enum loyalty{//default values would be 0,1,2
    GOLD_USER = 'GOLD_USER',
    SILVER_USER ='SILVER_USER',
    BRONZE_USER = 'BRONZE_USER'
}
export enum Permissions {
    ADMIN='admin',
    READ_ONLY = 12
}